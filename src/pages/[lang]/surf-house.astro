---
import { Image } from 'astro:assets';
import Layout from '../../layouts/Layout.astro';
import FeaturesGrid from '../../components/FeaturesGrid.astro';
import { houseFeatures } from '../../data/features';
import { useTranslations } from '../../i18n/utils';

// Import House Gallery
import house1 from '../../assets/images/surf-house-1.webp';
import house2 from '../../assets/images/surf-house-2.webp';
import house3 from '../../assets/images/surf-house-3.webp';

export function getStaticPaths() {
  return [{ params: { lang: 'fr' } }, { params: { lang: 'en' } }];
}

const { lang } = Astro.params;
const t = useTranslations(lang as any);
const gallery = [house1, house2, house3];
---

<Layout title={t('house.meta.title')}>
  <div class="bg-ocean text-sand py-20 px-4 text-center">
    <h1 class="text-4xl md:text-5xl font-serif font-bold">{t('house.hero.title')}</h1>
    <p class="mt-4 opacity-80 max-w-xl mx-auto font-light">{t('house.hero.desc')}</p>
  </div>

  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {gallery.map((img) => (
          <div class="rounded-2xl overflow-hidden h-64">
            <Image
              src={img}
              alt="Surf House Interior"
              class="w-full h-full object-cover hover:scale-105 transition duration-700"
              width={800}
              height={600}
            />
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="py-24 bg-light">
    <div class="max-w-4xl mx-auto px-4 text-center">
        <h2 class="text-3xl font-serif text-ocean mb-8">{t('house.main.title')}</h2>
        <p class="text-lg text-stone-600 leading-relaxed font-light mb-6">{t('house.main.p1')}</p>
        <p class="text-lg text-stone-600 leading-relaxed font-light">{t('house.main.p2')}</p>
    </div>
  </section>

  <div class="py-10">
    <FeaturesGrid
      features={houseFeatures.map(f => ({
        ...f,
        title: lang === 'en' ? (f.titleEn || f.title) : f.title,
        description: lang === 'en' ? (f.descriptionEn || f.description) : f.description
      }))}
      bgColor="bg-transparent"
      iconBgColor="bg-light"
    />
  </div>
</Layout>
